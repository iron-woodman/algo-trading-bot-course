# Курс по разработке алгоритмических торговых ботов

Данный репозиторий содержит материалы курса по созданию торговых роботов для криптовалютных рынков. Обучение построено по принципу "от простого к сложному", где каждый урок — это отдельный функциональный модуль или торговая стратегия.

## 📁 Структура проекта

Каждый урок находится в отдельной директории:

- **[Lesson_1 [history_loader]](./Lesson_1%20[history_loader])**: Синхронный загрузчик исторических данных (OHLCV) с биржи Bybit. Включает в себя фильтрацию инструментов по объему и автоматическую проверку/заполнение пропусков в данных.
- **Lesson_2 [history_loader]**: Продолжение разработки инструментов для работы с историей.

## 🛠 Технологический стек

- **Язык**: Python 3.10+
- **Биржи**: Работа через библиотеку `ccxt` (преимущественно Bybit).
- **Анализ данных**: `pandas`, `numpy`.
- **Конфигурация**: `python-dotenv` для безопасного хранения ключей API.

## 🚀 Быстрый старт

1. **Создайте виртуальное окружение:**
   ```bash
   python -m venv .venv
   ```

2. **Активируйте его:**
   - Windows: `.venv\Scripts\activate`
   - Linux/macOS: `source .venv/bin/activate`

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройка API:**
   В папке соответствующего урока скопируйте файл `.env.example` в `.env` и укажите свои ключи API:
   ```bash
   cp .env.example .env
   ```

## 📝 Правила оформления кода

Все разработки в рамках данного курса должны соответствовать стандартам, описанным в файле [GEMINI.md](./GEMINI.md). Основные требования:
- Обязательная аннотация типов.
- Подробные docstrings на русском языке.
- Использование асинхронности (где применимо).
- Чистота кода и следование принципам PEP8.

---
*Проект создан в образовательных целях.*
